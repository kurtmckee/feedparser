[tool.poetry]
package-mode = false

[project]
name = "dependencies"
requires-python = ">=3.10"
dependencies = [
    "coverage[toml]",
    "pytest",
    "pytest-randomly",
    "responses",
]

[dependency-groups]
coverage = [
    "coverage[toml]",
]
